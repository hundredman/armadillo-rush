# Armadillo Rush — 게임 디자인 문서

> 슬링으로 쏴라. 타이밍을 맞춰라. 최대한 높이 올라가라.

---

## 목차

1. [한 줄 핵심](#1-한-줄-핵심)
2. [왜 재미있는가](#2-왜-재미있는가)
3. [핵심 메카닉 — 속도 시스템](#3-핵심-메카닉--속도-시스템)
4. [게임 흐름](#4-게임-흐름)
5. [조작 방법](#5-조작-방법)
6. [지형 시스템](#6-지형-시스템)
7. [장애물 및 지형 파괴](#7-장애물-및-지형-파괴)
8. [난이도 시스템](#8-난이도-시스템)
9. [점수 시스템](#9-점수-시스템)
10. [타격감 시스템](#10-타격감-시스템)
11. [비주얼 설계](#11-비주얼-설계)
12. [기술 설계](#12-기술-설계)
13. [개발 순서](#13-개발-순서)
14. [리스크 및 대응](#14-리스크-및-대응)

---

## 1. 한 줄 핵심

**"바다를 박차고, 섬을 디디며, 달에 닿아라."**

바다 절벽 위의 슬링에서 발사된 아르마딜로가 공중에 떠 있는 섬들을 타고 위로 올라가 달을 목표로 한다.
섬에 착지하는 순간 타이밍에 맞게 가속하면 속도가 붙고,
속도가 높으면 장애물과 지형을 파괴하며 돌파할 수 있다.
속도가 부족하면 섬 사이 갭을 넘지 못하고 바다로 추락(SPLASH)한다.

### 세계 구조

```
🌕 달          ← 최종 목표 — MOON REACHED! (클리어)
   ↑ 달 궤도   (회청 글로우, 달 오브젝트 등장)
우주            (검정 배경, 별 & 은하수 가득)
성층권          (짙은 보라 → 검정 전환)
밤하늘          (남색, 첫 별 등장)
🌊 바다        ← 낙사 — SPLASH! (게임 오버)
```

---

## 2. 왜 재미있는가

### 핵심 재미 요소 3가지

#### 재미 1 — 타이밍의 긴장감
```
섬에 닿는 순간: "지금이다!" → 클릭/스페이스
타이밍 성공:    속도 +, 다음 갭을 넘을 수 있음
타이밍 실패:    속도 못 올림 → 갭에서 추락 위기
```
착지마다 판단이 필요하다. 리듬 게임처럼 타이밍이 실력 요소.

#### 재미 2 — 속도 = 파괴력 = 도약력
```
속도 높음 → 장애물/지형 파괴하며 돌파 + 갭을 멀리 날아 넘음
속도 낮음 → 장애물에 막힘 + 갭 못 넘고 추락
```
속도 하나가 모든 것을 결정한다. 단순하지만 깊다.

#### 재미 3 — 추락해도 부활 가능
```
갭에서 추락 → 아래 섬에 착지하면 부활 (콤보 0, 속도 절반 페널티)
아래 섬에서 다시 타이밍 맞춰 가속하면 재기 가능
아래에 섬이 없으면 게임 오버
```
"죽었다"가 아니라 "다시 올라가야 해" — 포기하지 않게 만드는 구조.
단 속도 절반 페널티로 추락이 공짜는 아니다 — 회복의 긴장감 유지.

### 유저 친화성 설계 원칙

```
1. 슬링 드래그 발사 — 당기는 방향 반대로 발사, 누구나 직관적으로 이해
2. 착지 타이밍 가속 — 클릭 하나로 완결
3. 속도 상태를 색상으로 명확히 표시 (빨강=고속, 회색=저속)
4. 첫 섬은 크고 갭이 좁음 — 자연스럽게 조작법 습득
5. 추락해도 아래 섬이 있으면 부활 — 즉각적인 좌절 없음
```

---

## 3. 핵심 메카닉 — 속도 시스템

### 속도 단위 정의 — 모든 물리의 기준

```
speedRatio: 0.0 ~ 1.0 (정규화 속도) — 게임 내 모든 속도의 단일 기준
  - 모든 속도 변화는 speedRatio에 대한 덧셈/뺄셈 (곱셈 아님)
  - 고속/저속, 파괴 임계값, 게임오버 판정 전부 이 값 기준
  - 1.0 초과 가속분은 1.0으로 클램프

actualSpeed (px/s) = speedRatio * MAX_SPEED   // 실제 물리 계산용 파생값
  - MAX_SPEED = 400 px/s (튜닝 상수)
  - 발사/구름/도약의 velocity 계산은 actualSpeed 사용

시간 기반 변화는 전부 "/초" 단위 + deltaTime 적용 (프레임레이트 무관)
  예: speedRatio -= FRICTION_PER_SEC * deltaTime
```

> **섬 체류 시간 목표 (밸런싱 기준)**: 섬당 0.8~1.5초.
> 이 목표에서 MAX_SPEED·섬 길이·마찰을 역산한다. 마찰/경사 효과가
> 체류 시간 안에 유의미하게 작동하도록 MAX_SPEED를 400px/s로 설정.
>
> | 구간 | 섬 길이 | 풀속도(1.0) 체류 | 저속(0.4) 체류 |
> |------|--------|----------------|---------------|
> | 초반 | 400px | 1.0초 | 2.5초 |
> | 후반 | 120px | 0.3초 | 0.75초 |
>
> 후반 풀속도 0.3초에도 마찰 -0.12×0.3 ≈ -0.036 작동, 오르막 누적 시 체감.
> 저속일수록 체류가 길어져 마찰이 강하게 작용 → 저속 탈출이 어려운 긴장감.

> 좌표·거리 환산: `1m = 10px` (게임 내부 px ↔ 표시 m 통일 계수).
> 갭 너비/섬 길이의 px 값, 높이/거리 카운터의 m 값 모두 이 계수로 변환한다.

### 속도 상태 2가지

#### 고속 상태
```
speedRatio >= 0.4 (HIGH_SPEED_THRESHOLD)
아르마딜로 색상: 빨간색 + 불꽃 trailing
장애물/지형 충돌 시: 파괴하며 돌파 (단, 재질 임계값도 충족해야 함 — §7)
갭 도달 시: 멀리 날아 넘음
```

#### 저속 상태
```
speedRatio < 0.4
아르마딜로 색상: 회색 + trailing 없음
장애물/지형 충돌 시: 막힘
갭 도달 시: 짧게 날아 추락 위험
```

> 고속/저속은 trailing·색상 등 연출 분기용 1차 기준이다.
> 실제 장애물 파괴 가능 여부는 재질별 파괴 임계값(§7)을 별도로 충족해야 한다.
> (예: speedRatio 0.5는 고속이지만 벽돌 임계값 0.7 미만이라 벽돌은 못 뚫음)

### 속도 변화 요인

> 모든 값은 `speedRatio`(0.0~1.0)에 대한 덧셈/뺄셈.
> 시간 기반(연속 효과)은 `/초` + deltaTime, 이벤트 기반(순간)은 1회 가감.

**시간 기반 — 연속 효과 (`/초`, deltaTime 곱함)**

| 요인 | speedRatio 변화 |
|------|---------|
| 지형 마찰 (구름) | -0.12/초 |
| 오르막 지형 | 추가 -0.06~-0.18/초 (경사 각도 비례) |
| 내리막 지형 | +0.06~+0.12/초 (경사 각도 비례) |

**이벤트 기반 — 순간 효과 (1회 가감)**

| 요인 | speedRatio 변화 |
|------|---------|
| 발사 직후 | 발사력에 비례 (0.0 ~ 1.0 설정) |
| 착지 타이밍 PERFECT | +0.30 |
| 착지 타이밍 GOOD | +0.15 |
| 착지 타이밍 OK | +0.05 |
| 착지 타이밍 MISS | +0.0 (입력 없거나 범위 밖) |
| 스프링 지형 통과 | +0.50 |
| 장애물 파괴 (고속) | -0.10~-0.20 (재질별) |
| 장애물 충돌 (저속, 막힘) | -0.50 (정지 위험) |
| 지형 파괴 통과 | -0.15 |

> 참고: `/초` 값은 60fps 기준 환산. (예: -0.12/초 ≈ -0.002/프레임 @60fps)
> deltaTime을 곱하므로 30fps에서도 동일한 초당 감소율 유지.

### 발사 각도와 속도 감소율

```
수평에 가까운 각도 (0~30도):
  - 초기 수평 속도 높음 → 첫 섬까지 빠르게 도달
  - 지형 마찰 영향 큼 (낮은 각도로 구름)

가파른 각도 (45~70도):
  - 높이 빠르게 획득
  - 착지 충격 흡수 손실 있음 (-10%)
  - 첫 섬 도달까지 체공 시간 김

최적 각도: 30~50도 (높이 + 수평 속도 균형)
```

### 착지 타이밍 가속 상세

```
판정 구간은 난이도별 timingWindow(W)에 비례 (착지 이후 시간만 카운트):
  0 ~ 0.4W   → PERFECT (speedRatio +0.30)
  0.4W ~ 0.7W → GOOD   (+0.15)
  0.7W ~ W   → OK      (+0.05)
  W 초과      → MISS    (+0.0)

예) 초반 W=0.5초 → PERFECT 0.2초 / GOOD~0.35초 / OK~0.5초
    후반 W=0.15초 → PERFECT 0.06초 / GOOD~0.105초 / OK~0.15초
  → 윈도우가 좁아져도 PERFECT/GOOD/OK 비율이 유지됨 (죽은 구간 없음)

착지 이전 입력 처리:
  착지 최대 0.1초 전 입력은 버퍼로 저장
  착지 발생 시 버퍼 입력이 있으면 GOOD으로 처리 (PERFECT 불가)
  → 미리 눌러서 쉽게 PERFECT 얻는 패턴 방지

타이밍 윈도우는 거리/높이에 따라 좁아짐 (난이도 섹션 참고)

연속 PERFECT 시 콤보 보너스 (speedRatio 가산분에 추가):
  2연속: 가산분 +0.05 추가
  3연속: 가산분 +0.10 추가
  5연속+: 가산분 +0.20 추가 + 아르마딜로 색상 변화 (황금색)
  ※ 속도 보너스에만 적용. 점수 콤보 보너스(§9)와는 별개 시스템.
```

---

## 4. 게임 흐름

### 전체 구조

```
[타이틀 화면]
  - 베스트 기록 (최고 높이 + 최고 점수) 표시
  - 탭/클릭으로 즉시 시작
    ↓
[슬링 준비 — SLINGING 상태]
  - 아르마딜로가 슬링 포켓에 장전
  - 드래그로 각도 + 발사력 동시 설정 (당긴 방향의 반대로 발사)
  - Y자 슬링 + 고무줄 비주얼, 점선 궤적 미리보기
  - 손/버튼 떼면 발사 (최소 당김 미달 시 취소)
    ↓
[비행 단계 — FLYING 상태]
  - Planck.js 물리: 중력·충돌·탄성 처리
  - 고속으로 지형 하단을 위로 통과 시 지형 파괴
  - 첫 섬 도달 실패 시 → 슬링으로 자동 복귀하여 재발사
    → 첫 섬은 항상 도달 가능 위치에 배치 (튜토리얼 성격)
    ↓
[구름 단계 — 무한 반복]
  - 지형 곡선에 따라 자동으로 구름
  - 착지 타이밍에 맞춰 클릭/스페이스 → 가속
  - 장애물/지형 충돌 → 고속이면 파괴, 저속이면 막힘
  - 섬 끝 갭 도달 → 속도에 따라 날아서 넘거나 추락
    ↓
[추락 시]
  - 아래 섬에 착지 → 부활, 구름 단계 재개
  - 아래에 섬 없음 → 게임 오버
    ↓
[게임 오버]
  - 최종 높이 + 거리 + 점수 표시 (§9 목업)
  - 최고 높이 / 최고 점수 갱신 여부 표시
  - 즉시 재시도
```

### 구름 단계 상세

```
섬 착지 후:
  - 지형 곡선을 따라 자동으로 구름
  - 내리막: 자동 가속 → 갭 도약 준비
  - 오르막: 자동 감속 → 타이밍 가속 필요
  - 착지 순간: 타이밍 판정 발생

섬 끝(갭) 도달:
  - 현재 속도에 따라 포물선으로 날아감
  - 다음 섬에 닿으면 착지 → 타이밍 판정
  - 못 닿으면 추락 → 아래 섬 유무 판정

게임 오버 판정:
  - 추락 중 아래에 섬이 없으면 게임 오버
  - 속도 정지 판정: speedRatio <= 0.05 상태가 2초 이상 지속되면 게임 오버
      → 1초 경과 시 "DANGER" 경고 UI 표시
      → 지형 내리막/스프링으로 2초 내 speedRatio > 0.05 회복 시 판정 취소

부활 판정 (추락 중 아래 섬 착지):
  - 콤보 0으로 리셋 (추락은 실수 → 페널티)
  - speedRatio 절반으로 감소 (착지 직전 값 × 0.5)
  - 착지 즉시 타이밍 판정 재개 → 회복 기회 부여
```

---

## 5. 조작 방법

### 설계 원칙
- 발사: 슬링 드래그 1번 (당기는 방향 반대로 발사)
- 구름 중: 클릭/탭 1번 (타이밍 가속)
- 총 2가지 동작으로 완결
- 모바일 한 손 플레이 가능

### 모바일 조작

| 단계 | 입력 | 동작 |
|------|------|------|
| 발사 준비 | 슬링 드래그 | 각도 + 발사력 동시 설정 |
| 발사 | 손 떼기 | 아르마딜로 발사 |
| 구름 중 | 화면 탭 | 착지 타이밍 가속 |

### PC 조작

| 단계 | 입력 | 동작 |
|------|------|------|
| 발사 준비 | 마우스 드래그 | 각도 + 발사력 동시 설정 |
| 발사 | 마우스 버튼 놓기 | 아르마딜로 발사 |
| 구름 중 | 클릭 or Space | 착지 타이밍 가속 |
| Space (SLINGING) | Space | 45도 고정 풀파워 즉시 발사 |

### 유저 편의 세부 사항

- **슬링 비주얼**: Y자 나무 슬링 + 고무줄이 당김 위치로 실시간 갱신
- **궤적 점선 미리보기**: 드래그 중 포물선 궤도 점선 표시
- **발사력 게이지**: 드래그 길이로 발사력 조절, HUD에 % 표시
- **타이밍 인디케이터**: 착지 이후 윈도우가 줄어드는 게이지 바로 표시
- **속도계 UI**: 아르마딜로 색상 + HUD 즉시 확인
- **높이/거리 카운터**: 상단 항상 표시
- **재시도 버튼**: 항상 화면 하단 노출 — 즉시 재시작
- **드래그 최소 거리**: 18px threshold — 오발사 방지
- **콤보 표시**: 연속 PERFECT 횟수 화면에 표시 (PERFECT 외 판정 시 0으로 리셋)

---

## 6. 지형 시스템

### 지형 구조 — 공중 섬

```
섬들이 공중에 떠 있고 위로 이어짐
섬과 섬 사이에는 갭(빈 공간)이 있음
아르마딜로는 섬 위를 구르다가 갭에서 날아서 다음 섬으로
```

```
                    [섬 E] ← 더 높이, 더 좁음
                   /
              [섬 D]
             /      \(갭)
        [섬 C]
       /      \(갭)
  [섬 B]
 /      \(갭)
[섬 A] ← 첫 착지
  |
[슬링]
```

### 섬 형태 — 3종 (모두 매끄러운 CatmullRom 스플라인)

모든 섬은 울퉁불퉁함 없이 매끄러운 곡선으로 생성된다.
아르마딜로가 자연스럽게 가속/감속하며 구를 수 있는 형태만 허용.

| 형태 | 모양 | 특징 | 전략 |
|------|------|------|------|
| **Bowl (그릇)** | U자 — 중심 낮고 양 끝 높음 | 중심으로 자연 가속 → 양쪽 경사면에서 감속 | 기본 섬, 타이밍 가속이 중요한 구간 |
| **Ramp (경사)** | 왼쪽 낮고 오른쪽 높은 오르막 | 진입 시 자연 가속 구간 없음, 끝에서 높이 획득 | 도약 준비 구간 |
| **Wave (파도)** | S커브 — 낮은 입구 → 중간 고원 → 높은 출구 | 중간 평탄 구간에서 타이밍 여유 | 콤보 쌓기 쉬운 구간 |

절차적 생성 시 Bowl 위주로 배치하되 가끔 Ramp/Wave 삽입 (`bowl → bowl → ramp → bowl → wave → ...` 순환).

### 지형 곡선 종류 및 속도 효과

| 곡선 종류 | 형태 | 속도 효과 | 전략적 역할 |
|---------|------|---------|-----------|
| 완만한 오르막 | ／ | -1%/프레임 | 기본 이동 |
| 가파른 오르막 | ｜ | -3%/프레임 | 타이밍 가속 필수 구간 |
| 완만한 내리막 | ＼ | +1%/프레임 | 자연 가속, 갭 도약 준비 |
| 가파른 내리막 | ↘ | +2%/프레임 | 빠른 가속, 과속 주의 |
| S자 곡선 | ∫ | 오르막 후 내리막 | 타이밍 2회 연속 판정 |
| 점프대 | ↗ | 속도 → 높이 변환 (vy↑, speedRatio 유지) | 큰 갭 도약 전용 |
| 스프링 지형 | ⤒ | speedRatio +0.50 순간 부스트 (vy 변화 없음) | 특수 가속 지점 |
| 파괴 가능 지형 | ░ | 고속 시 -0.15, 통과 | 장애물 겸 지형 |

> **점프대 vs 스프링 차이 (§12 코드 반영)**:
> - 점프대: 도약 시 `launchAngle`을 강제로 높임(60~75도) → 수평속도 일부를 수직속도로 전환. 높이 획득용.
> - 스프링: `launchAngle`은 지형 접선 그대로, `speedRatio`만 +0.50 부스트 → 도약 거리 증가. 거리 획득용.

### 섬 크기 및 갭 설계

```
(1m = 10px 환산 기준)

초반 섬 (낮은 높이):
  - 섬 길이: 300~500px (30~50m)
  - 갭 너비: 80~120px (8~12m)
  - 곡선: 완만한 오르막/내리막 위주

중반 섬 (중간 높이):
  - 섬 길이: 150~300px (15~30m)
  - 갭 너비: 120~200px (12~20m)
  - 곡선: S자, 가파른 구간 등장

후반 섬 (높은 높이):
  - 섬 길이: 80~150px (8~15m)
  - 갭 너비: 200~350px (20~35m)
  - 곡선: 점프대, 파괴 가능 지형 혼합
```

### 플레이어블리티 보장 — 갭 생성 규칙

절차적 생성 시 현재 아르마딜로 최대 도달 가능 속도 기준으로 넘을 수 없는 갭은 생성하지 않는다.

```javascript
// 갭 생성 전 통과 가능 여부 검증
// gapWidth, heightDiff: px 단위 / maxReachableRatio: 해당 섹션 기대 최대 speedRatio
function isGapPlayable(gapWidth, heightDiff, maxReachableRatio) {
  const maxSpeed = maxReachableRatio * MAX_SPEED        // px/s 로 환산
  const launchAngle = Math.PI / 4                       // 45도 (일반 섬 끝 도약 기준)
  const vx = maxSpeed * Math.cos(launchAngle)
  const vy = maxSpeed * Math.sin(launchAngle)
  // heightDiff: 다음 섬이 현재 섬보다 높으면 양수 (도달이 더 어려움)
  // 포물선 방정식: 0 = vy*t - 0.5*GRAVITY*t² - heightDiff
  // → t = (vy - sqrt(vy² - 2*GRAVITY*heightDiff)) / GRAVITY  (착지 시각, 작은 근)
  const discriminant = vy * vy - 2 * GRAVITY * heightDiff
  if (discriminant < 0) return false                    // 물리적으로 도달 불가
  const flightTime = (vy - Math.sqrt(discriminant)) / GRAVITY
  const maxReach = vx * flightTime
  return gapWidth < maxReach * 0.85                      // 15% 안전 마진
}
// ※ 점프대가 직전에 있으면 launchAngle을 높여(60~75도) heightDiff가 큰 갭도 검증.
//   스프링이 있으면 maxReachableRatio에 +0.50 반영 후 검증.
```

- 검증 실패 시: 갭 너비를 줄이거나 갭 앞에 스프링 지형 자동 배치
- 연속 2개 갭이 모두 최대치인 경우 두 번째 갭 앞에 스프링 강제 삽입

---

## 7. 장애물 및 지형 파괴

### 장애물 종류

> 파괴 임계값(§ 재질별 임계값) = 파괴에 필요한 최소 speedRatio.
> 속도 감소는 speedRatio 1회 차감.

| 장애물 | 설명 | 파괴 임계값 | 파괴 시 감소 | 막힘 시 감소 |
|--------|------|-----------|------------|------------|
| **나무 블록** | 섬 위에 놓인 기본 장애물 | 0.30 (나무) | -0.10 | -0.50 |
| **돌 블록** | 중간 강도 | 0.50 (돌) | -0.20 | -0.50 |
| **이동 블록** | 좌우로 움직이는 블록 (나무 재질) | 0.30 (나무) | -0.15 | -0.50 |
| **철제 블록** | 파괴 불가 (금속 재질) | 1.0 초과 (불가) | — | -0.40 (항상 막힘) |
| **가시 지형** | 섬 표면에 박힌 가시 (파괴 대상 아님) | — | — | 통과, 항상 -0.15 |

> - 가시: 파괴/막힘 개념 없이 통과하되 speedRatio -0.15 (고속/저속 무관).
> - 철제: 어떤 속도에서도 파괴 불가, 충돌 시 항상 -0.40 후 막힘(정지 위험).
> - 이동 블록: 나무 재질이라 임계값은 같으나, 좌우 이동으로 충돌 타이밍이 가변.

### 지형 파괴 시스템

```
파괴 가능 지형 (░ 표시, 재질별 임계값 적용):
  - speedRatio >= 재질 임계값: 지형 파괴 + 통과 + 파편 파티클
  - speedRatio < 재질 임계값: 막힘, 파괴 안 됨
  - 파괴된 지형은 사라져 갭이 생김 (추락 위험)

연쇄 파괴:
  - 파괴 가능 지형이 연속으로 배치된 경우
  - 첫 번째 파괴 충격이 다음으로 전달
  - 최대 3단계 연쇄
```

### 재질별 파괴 임계값

> 파괴 임계값은 `speedRatio` 기준. 충돌 시점 speedRatio가 임계값 이상이면 파괴, 미만이면 막힘.

| 재질 | 파괴 임계값 (speedRatio) | 색상 |
|------|--------------|------|
| 유리 | 0.10 | 투명 하늘색 |
| 나무 | 0.30 | 갈색 |
| 돌 | 0.50 | 회색 |
| 벽돌 | 0.70 | 주황 |
| 금속 | 1.0 초과 (사실상 파괴 불가) | 은색 |

---

## 8. 난이도 시스템

### 2축 난이도 — 높이 × 거리

```
난이도 = f(높이, 거리)
두 값 중 높은 쪽이 난이도를 주도
```

### 높이 축 — 섬 구조 난이도

| 높이 구간 | 섬 크기 | 갭 너비 | 장애물 | 지형 곡선 |
|---------|--------|--------|--------|---------|
| 0~50m | 크고 넓음 | 좁음 | 없음 | 완만한 오르막/내리막 |
| 50~150m | 중간 | 보통 | 나무/돌 블록 | S자 등장 |
| 150~300m | 작아짐 | 넓어짐 | 이동 블록 등장 | 가파른 구간 |
| 300~500m | 좁고 작음 | 더 넓음 | 철제 블록, 가시 | 점프대 필수 |
| 500m+ | 매우 좁음 | 매우 넓음 | 혼합 | 파괴 가능 지형 |

### 거리 축 — 타이밍 난이도

> 시간 기준이 아닌 **실제 이동 거리(m)** 기준으로 산정.
> 같은 시간이라도 플레이어 실력에 따라 이동 거리가 달라지므로 거리 기반이 일관성 있음.

> 타이밍 윈도우(W)는 §3 비례 판정의 기준값. 각 구간에서
> PERFECT=0~0.4W, GOOD=0.4~0.7W, OK=0.7~W로 자동 환산된다.
> "PERFECT 콤보 권장"은 속도 보너스(§3) 획득 권장치이며 생존 필수는 아니다.

| 이동 거리 구간 | 타이밍 윈도우 W | PERFECT 구간(0.4W) | PERFECT 콤보 권장 | 스프링 빈도 |
|------------|------------|------------|------------|-----------|
| 0~300m | 0.5초 (넓음) | 0.20초 | — | 많음 |
| 300~800m | 0.35초 | 0.14초 | 3연속 | 보통 |
| 800~1,500m | 0.25초 | 0.10초 | 5연속 | 적음 |
| 1,500m+ | 0.15초 (좁음) | 0.06초 | 지속 | 없음 |

### 마일스톤 알림

```
높이 도달 시:
  50m  → "GOOD!"
  150m → "GREAT!"
  300m → "AMAZING!"
  500m → "LEGENDARY!"
```

---

## 9. 점수 시스템

### 점수 계산

```
메인 점수
├── 도달 높이: 1m = 100점
└── 이동 거리: 1m = 10점

보너스 점수
├── 타이밍 PERFECT: +500점
├── 타이밍 GOOD: +200점
├── 연속 PERFECT 콤보: 콤보 수 × 300점
├── 장애물/지형 파괴: 재질별 100~500점
│   유리 200 / 나무 100 / 돌 300 / 벽돌 500
│   ※ 금속(철제)은 파괴 불가 → 파괴 점수 없음 (§7과 일치)
└── 연쇄 파괴: 파괴 수 × 500점 추가
```

### 게임 오버 화면

```
┌─────────────────────────┐
│       GAME OVER         │
│                         │
│   높이:    347m         │
│   거리:    1,203m       │
│   점수:    284,500      │
│                         │
│   최고 높이:  412m      │
│   최고 점수:  310,200   │
│                         │
│        [재시도]         │
└─────────────────────────┘
```

> 베스트 기록은 높이와 점수를 각각 독립적으로 저장한다.
> (높이 베스트 달성 시와 점수 베스트 달성 시 갱신 메시지를 별도 표시)

---

## 10. 타격감 시스템

### 착지 타이밍 성공 시

```
PERFECT 판정:
  1. 아르마딜로 주변 폭발 파티클 (방사형, 8개)
  2. "PERFECT!" 텍스트 팝업 (위로 상승 후 사라짐)
  3. 화면 플래시 (노란색, 0.05초)
  4. 모바일: 짧은 진동 20ms

GOOD 판정:
  1. 작은 파티클 (4개)
  2. "GOOD!" 텍스트 팝업

OK 판정:
  1. 파티클 없음
  2. "OK" 텍스트 팝업 (작은 크기, 빠르게 사라짐)

MISS 판정:
  1. 아르마딜로 짧은 흔들림 (x축 ±3px, 0.1초)
  2. 피드백 없음 — 플레이어가 타이밍을 놓쳤음을 인지하게만 함
```

### 장애물/지형 파괴 시

```
충돌 순간 (0ms):
  1. 충돌 지점 화면 플래시 (흰색, 0.05초)
  2. 슬로우모션 (timeScale = 0.3, 0.08초)
  3. 카메라 셰이크 (trauma 추가)
  4. 모바일: 진동 (나무 20ms / 돌 40ms / 금속 80ms)

슬로우모션 해제 (80ms):
  5. 파편 파티클 폭발 (재질별 색상, 10~20개)
  6. 먼지 구름 (3~5프레임)
  7. 연쇄 파괴 시: trauma +0.1씩 추가
```

### 카메라 셰이크 (Perlin Noise 기반)

```
trauma 기반 강도:
  나무 파괴: +0.2 / 돌: +0.3 / 벽돌: +0.4 / 금속: +0.5
  연쇄 파괴: 파괴당 +0.1 추가

최대 trauma: 0.8
셰이크 강도 = trauma²
감쇠: 매 프레임 trauma -= 0.05
방향: Perlin Noise로 x/y 독립 계산
모바일 저사양: 셰이크 강도 50% 감소
```

### 파편 파티클 — 재질별

| 재질 | 파편 색상 | 파편 수 | 크기 | 특징 |
|------|---------|--------|------|------|
| 나무 | 갈색 #8D6E63 | 8~12개 | 작음 | 나뭇결 파편 |
| 돌 | 회색 #78909C | 10~15개 | 중간 | 불규칙 다각형 |
| 벽돌 | 주황 #FF7043 | 8~10개 | 중간 | 직사각형 파편 |
| 유리 | 투명 #B3E5FC | 15~20개 | 매우 작음 | 반짝이는 투명 파편 |
| 금속 | 은색 #B0BEC5 | 6~8개 | 큼 | 불꽃 튀김 + 광택 |

### 추락 연출

```
갭에서 추락 시:
  1. 아르마딜로 회전하며 낙하
  2. 카메라가 천천히 따라 내려옴
  3. 아래 섬 착지 시: 먼지 파티클 + 착지음
  4. 게임 오버 시: 화면 페이드 아웃 (0.5초)
```

### 슬로우모션 쿨다운

```
쿨다운: 0.3초
연쇄 파괴 중 추가 슬로우 억제 (첫 충돌 1회만)
```

---

## 11. 비주얼 설계

### 시점 및 카메라

- **2D 사이드뷰** — 위로 스크롤 (수직 + 수평 혼합)
- **카메라 타입: OrthographicCamera** (완전 2D 평면 렌더링)
  - PerspectiveCamera 대비 원근 왜곡 없음 → 타이밍 판정 시 위치 예측이 직관적
  - 셰이더 좌표계: 모든 position 계산을 화면 픽셀 단위로 통일
  - 카메라 셰이크: orthographic의 left/right/top/bottom 오프셋 직접 조작 (view matrix 변환 불필요)
- 아르마딜로를 카메라가 추적 (lerp 계수 0.12)
- 추락 시 카메라가 아래 섬까지 따라 내려옴
- 게임 오버 시 0.5초 유지 후 결과 화면
- 구조물 파괴 시 카메라 셰이크 (Perlin Noise)

### 색상 테마

```
하늘 배경:         어두운 밤하늘 → 높이 올라갈수록 우주색으로 변화
                   #1A237E (낮은 높이) → #000011 (높은 높이)
섬 지형:           초록 잔디 + 갈색 흙, 공중에 떠 있는 느낌
아르마딜로 (고속): 빨간색 #FF1744 + 주황 불꽃 trailing
아르마딜로 (저속): 회색 #757575 + trailing 없음
아르마딜로 (콤보): 황금색 #FFD600 (5연속 PERFECT)
스프링 지형:       밝은 초록 #76FF03 + 발광
장애물:            재질별 고유 색상
UI:                흰색 텍스트 + 검정 테두리
```

### 커스텀 GLSL 셰이더 목록

Three.js ShaderMaterial / RawShaderMaterial로 직접 GLSL 작성.

| 셰이더 | 적용 대상 | 구현 방식 | 수업 개념 |
|--------|---------|---------|---------|
| Phong Shading | 아르마딜로, 대포, 장애물 | RawShaderMaterial + GLSL | 조명 모델 (ambient + diffuse + specular) |
| 파티클 셰이더 | 불꽃 trailing, 파괴 파편, 먼지 | Points + ShaderMaterial | 빌보드, 블렌딩 (additive) |
| 텍스처 매핑 | 배경, 섬 재질, 지형 | ShaderMaterial + uniform sampler2D | UV 매핑, 타일링 |
| 글로우/블룸 셰이더 | 스프링 지형, 콤보 상태 | WebGLRenderTarget + 포스트프로세싱 | 프레임버퍼, 블룸 |
| 배경 그라데이션 | 하늘 배경 (높이에 따라 변화) | fragment shader uniform (height) | 프래그먼트 셰이더 uniform |
| 속도 기반 색상 보간 | 아르마딜로 상태 전환 | fragment shader uniform (speedRatio) | 프래그먼트 셰이더 uniform |

### 이펙트 디테일

> 파티클은 두 계열로 구분: ① 지속 trailing(고속 시 꼬리, 15~20개 상시 유지)
> ② 순간 폭발(PERFECT/파괴 시 1회 방출, 8~20개). 둘은 별개 풀에서 관리.

```
아르마딜로 고속 상태:
  - 빨간색 + 오렌지/노란 불꽃 trailing (지속, 파티클 15~20개)
  - 속도에 비례한 trailing 길이

타이밍 PERFECT:
  - 황금색 폭발 파티클
  - "PERFECT!" 텍스트 팝업

콤보 5연속+:
  - 아르마딜로 황금색 + 발광 효과
  - 콤보 수 화면 표시

스프링 지형 통과:
  - 초록색 빛 폭발
  - 속도 게이지 급상승 연출

추락 중:
  - 아르마딜로 회전 애니메이션
  - 카메라 천천히 하강
```

---

## 12. 기술 설계

### 기술 스택

```
렌더링:          Three.js r171 (WebGL 2.0 기반)
포스트프로세싱:   postprocessing ^6 (EffectComposer — Bloom, ChromaticAberration, Vignette)
물리 엔진:       Planck.js ^1 (Box2D 포트 — 비행·추락 구간 담당)
셰이더:          커스텀 GLSL (ShaderMaterial — particle, crater, background)
빌드:            Vite 6 (ESM, ?raw GLSL import)
언어:            Vanilla JavaScript (ES Modules)
플랫폼:          PC + 모바일 브라우저
사운드:          Web Audio API (WebAudioContext oscillator, 최소 효과음)
시간:            고정 deltaTime(1/60s) 누적 방식 물리 루프.
                 프레임레이트 무관 동작 보장.
```

### 물리 시스템 설계

#### 발사 물리 (슬링)

```javascript
// 슬링 드래그 → 발사 각도 + 파워 계산
// 당긴 방향의 정반대가 발사 방향
const raw = worldPos.sub(SLING_POS)           // 터치 - 슬링 중심
const len = Math.min(raw.length(), SLING_MAX_PULL)
const slingAngle = Math.atan2(-raw.y, -raw.x) // 반대 방향

// 당김 거리로 파워 선형 보간 (SLING_POWER_MIN=0.78 ~ SLING_POWER_MAX=1.0)
const slingPower = lerp(SLING_POWER_MIN, SLING_POWER_MAX,
  (len - SLING_MIN_PULL) / (SLING_MAX_PULL - SLING_MIN_PULL))

// 발사
const speed = slingPower * LAUNCH_SPEED       // LAUNCH_SPEED = 850 px/s
velocity.x = Math.cos(slingAngle) * speed
velocity.y = Math.sin(slingAngle) * speed

// Planck body에 동일 속도 설정 (비행 물리는 Planck가 담당)
physics.setArmadilloPos(armadillo.position)
physics.setArmadilloVelocity(velocity.x, velocity.y)
```

#### 구름 물리 (지형 위)

```javascript
// 지형 곡선 접선 방향으로 이동 (actualSpeed로 위치 갱신)
const tangent = terrain.getTangentAt(position)
velocity = tangent.multiplyScalar(speedRatio * MAX_SPEED)

// 경사 각도에 따른 속도 변화 — 전부 /초 단위, deltaTime 곱함
const slope = terrain.getSlopeAt(position)        // -1(내리막) ~ +1(오르막)
const FRICTION_PER_SEC = 0.12                      // 마찰 -0.12/초
const SLOPE_RESIST_PER_SEC = 0.18                  // 경사 최대 저항 -0.18/초
speedRatio -= slope * SLOPE_RESIST_PER_SEC * deltaTime  // 오르막 감속/내리막 가속
speedRatio -= FRICTION_PER_SEC * deltaTime
speedRatio = Math.max(0, Math.min(1, speedRatio))  // 0~1 클램프
```

#### 착지 타이밍 판정

```javascript
// 착지 이벤트 발생 시
const timingWindow = getTimingWindow(height, distance)  // 난이도별 윈도우

// 착지 이전 버퍼 입력 처리 (최대 0.1초 전 입력 → GOOD으로 격하)
if (bufferedInput && inputTime < landingTime) {
  rating = 'GOOD'
  bufferedInput = false
} else {
  const timeDelta = inputTime - landingTime  // 착지 이후 경과 시간 (음수면 무효)
  const W = timingWindow                      // 난이도별 윈도우
  if (timeDelta < 0) rating = 'MISS'
  else if (timeDelta < W * 0.4) rating = 'PERFECT'
  else if (timeDelta < W * 0.7) rating = 'GOOD'
  else if (timeDelta < W)       rating = 'OK'
  else rating = 'MISS'
}

const speedBonus = { PERFECT: 0.30, GOOD: 0.15, OK: 0.05, MISS: 0.0 }

// 콤보는 "연속 PERFECT" 카운트 (PERFECT 외엔 리셋 — §3 정의와 일치)
combo = (rating === 'PERFECT') ? combo + 1 : 0

// 콤보 가산분 (연속 PERFECT 시 추가, speedRatio에 더함)
let comboBonus = 0
if (combo >= 5) comboBonus = 0.20
else if (combo >= 3) comboBonus = 0.10
else if (combo >= 2) comboBonus = 0.05

speedRatio += speedBonus[rating] + comboBonus  // 덧셈 (곱셈 아님)
speedRatio = Math.min(1, speedRatio)            // 1.0 클램프
```

#### 갭 도약 물리

```javascript
// 섬 끝에서 포물선 도약
// launchAngle 결정 규칙:
//   - 일반 섬 끝: 지형 접선 각도 (terrain.getTangentAngleAt(끝점))
//                 단 내리막 끝은 접선이 음수 → MIN_LAUNCH_ANGLE(15도)로 하한 보장
//                 (음수 각도면 즉시 추락하므로 최소 상승 각 강제)
//   - 점프대(↗):  60~75도로 강제 (수평→수직 변환, 높이 획득)
//   - 스프링(⤒):  접선 그대로(하한 적용), 대신 speedRatio +0.50 선반영
const MIN_LAUNCH_ANGLE = Math.PI / 12               // 15도
let launchAngle = getLaunchAngle(terrainEndType)    // 위 규칙
if (terrainEndType === 'normal' || terrainEndType === 'spring') {
  launchAngle = Math.max(launchAngle, MIN_LAUNCH_ANGLE)  // 음수/저각 방지
}
const actualSpeed = speedRatio * MAX_SPEED
velocity.x = actualSpeed * Math.cos(launchAngle)
velocity.y = actualSpeed * Math.sin(launchAngle)

// 매 프레임 (공중 비행 중 speedRatio는 변하지 않음, 위치만 갱신)
velocity.y -= GRAVITY * deltaTime
position.x += velocity.x * deltaTime
position.y += velocity.y * deltaTime
```

#### 파괴 판정

```javascript
// 아르마딜로-장애물/지형 충돌 (질량 개념 없이 speedRatio로 단순화)
if (speedRatio >= obstacle.breakThreshold) {   // 재질 임계값(§7) 충족
  obstacle.destroy()
  speedRatio -= obstacle.speedReduction         // 파괴 통과 (재질별 0.10~0.20)
} else {
  speedRatio -= obstacle.bounceSpeedReduction   // 막힘 (-0.50, 정지 위험)
}
speedRatio = Math.max(0, speedRatio)
```

### 파일 구조 (현재 구현 기준)

```
/armadillo-rush
├── index.html
├── vite.config.js
└── src/
    ├── main.js               # 게임 루프(fixed-dt), 상태 머신, 슬링 시스템, HUD
    ├── state.js              # StateMachine (TITLE→SLINGING→FLYING→ROLLING⇄FALLING→GAMEOVER)
    ├── config.js             # 물리 상수, 점수 설정, 1m=10px 환산
    ├── assets.js             # Kenney 스프라이트 로더
    ├── ui.css                # HUD, 게이지, 결과 화면 스타일
    ├── game/
    │   ├── terrain.js        # Bowl/Ramp/Wave 지형 생성 (CatmullRom)
    │   │                     #   + 절차적 섬 생성 (generateNextIslandSpec)
    │   │                     #   + 지형 파괴 (damageTerrain, ShaderMaterial crater)
    │   │                     #   + 파편 물리 애니메이션 (updateTerrainChunks)
    │   ├── particles.js      # InstancedMesh 파티클 (512슬롯, GPU 인스턴싱)
    │   └── physics.js        # Planck.js 래퍼 (비행·추락 — dynamic Circle + static Chain)
    ├── renderer/
    │   ├── scene.js          # Renderer + OrthographicCamera + resize 처리
    │   ├── background.js     # 배경 그라데이션 셰이더 (높이 → 밤하늘/우주)
    │   └── postfx.js         # EffectComposer: BackgroundPass→RenderPass→Bloom+CA+Vignette
    └── shaders/
        ├── particle.vert     # 인스턴스 파티클 (aLife, aColor 속성)
        ├── particle.frag     # 이차 페이드 + AdditiveBlending
        ├── crater.vert       # UV passthrough
        ├── crater.frag       # FBM 노이즈 크레이터 엣지 + 생성 플래시
        ├── background.vert
        └── background.frag
```

### 성능 최적화

```
- 오브젝트 풀링: 파티클 300개 풀 고정 (Three.js Points geometry 재사용)
- 충돌 감지: 활성 오브젝트만 (파괴된 장애물 즉시 scene에서 제거)
- 화면 밖 오브젝트 제거: 아르마딜로보다 아래로 벗어난 섬 즉시 삭제
- 연쇄 파괴 깊이 제한: 최대 3단계
- Three.js Frustum Culling: 화면 밖 자동 렌더링 스킵
- 모바일 저사양 모드 자동 전환 (FPS < 40):
    - 파티클 수 50% 감소
    - 글로우 WebGLRenderTarget 해상도 절반
    - 카메라 셰이크 강도 감소
```

---

## 13. 개발 순서

> **원칙: MVP 우선 → 타격감 후처리**
> 1~5단계로 플레이 가능한 상태를 만든 뒤, 6~8단계에서 셰이더/이펙트를 붙인다.
> 셰이더 1종(특히 블룸)만으로 하루가 소요될 수 있으므로 단계별 절충안을 명시한다.

### 1단계 — 렌더링 기반 (3일)
- [ ] Three.js Scene 초기화, 캔버스 세팅 (**OrthographicCamera** 확정)
- [ ] 배경 (하늘, 높이에 따른 색상 변화 셰이더)
- [ ] 섬 지형 기본 렌더링 + 텍스처 매핑
- [ ] Phong Shading (아르마딜로, 장애물)
- [ ] 카메라 추적 시스템 (위로 스크롤)

### 2단계 — 발사 + 기본 물리 (3일)
- [ ] 대포 드래그 발사 + 궤적 미리보기
- [ ] 포물선 비행 물리 (중력)
- [ ] 첫 섬 착지 판정
- [ ] 지형 곡선 이동 물리 (접선 방향)

### 3단계 — 속도 시스템 + 타이밍 (3일)
- [ ] 속도 상태 (고속/저속) + 아르마딜로 색상 변화 셰이더
- [ ] 착지 타이밍 판정 (PERFECT/GOOD/OK/MISS) + 버퍼 입력 처리
- [ ] 콤보 시스템
- [ ] 갭 도약 물리
- [ ] 속도 5% 이하 2초 지속 → 게임 오버 판정 + DANGER UI

### 4단계 — 장애물 + 지형 파괴 (3일)
- [ ] 장애물 5종 + 파괴 판정 (AABB)
- [ ] 지형 파괴 시스템
- [ ] 연쇄 파괴 (최대 3단계)
- [ ] 추락 + 부활 + 게임 오버 판정

### 5단계 — 절차적 생성 + 난이도 (3일)
- [ ] 절차적 섬/장애물 생성 (높이+거리 파라미터)
- [ ] **플레이어블리티 검증** (`isGapPlayable`) + 스프링 자동 배치
- [ ] 타이밍 윈도우 난이도 조절 (이동 거리 기반)
- [ ] 높이/거리 카운터 + 점수 시스템
- [ ] 마일스톤 알림

### 6단계 — 기본 타격감 (3일)
- [ ] 파티클 시스템 (불꽃 trailing, 재질별 파편, 먼지)
- [ ] 슬로우모션 (timeScale 기반, 0.08초)
- [ ] 카메라 셰이크 (Perlin Noise trauma)
- [ ] 화면 플래시 + 타이밍 판정 이펙트
- [ ] 모바일 진동 API

### 7단계 — 고급 셰이더 이펙트 (4일) ★이전 계획 대비 1일 추가
- [ ] 글로우/블룸 셰이더 (WebGLRenderTarget — 가장 구현 난이도 높음, 먼저 착수)
- [ ] 스프링 지형 발광, 콤보 황금색 발광
- [ ] 속도 기반 trailing 길이 셰이더
- [ ] **절충안**: 블룸이 시간 초과 시 CSS filter: blur() 기반 소프트 글로우로 대체

### 8단계 — 완성도 (3일)
- [ ] 게임 오버 화면 + 베스트 기록
- [ ] 모바일 터치 최적화
- [ ] 저사양 모드 자동 전환 (FPS < 40)
- [ ] 전체 밸런싱 + 버그 수정 + 폴리싱

**총 개발 기간: 약 23일** (이전 21일 → 셰이더 단계 현실화로 2일 증가)

---

## 14. 리스크 및 대응

| 리스크 | 가능성 | 대응 방법 |
|--------|--------|---------|
| 지형 곡선 물리 구현 난이도 | 높음 | 베지어 곡선 or 스플라인으로 단순화, 충분한 단위 테스트 |
| 타이밍 판정 체감이 부정확 | 높음 | 타이밍 윈도우 config로 분리, 플레이 테스트로 조정 |
| 절차적 생성 패턴 단조로움 | 높음 | 섬 패턴 사전 설계 + 랜덤 조합 |
| **블룸 셰이더 구현 일정 초과** | **높음** | **7단계에 4일 배정 + 절충안(CSS filter)으로 기능 보장** |
| **넘을 수 없는 갭 생성** | **중간** | **`isGapPlayable` 검증 + 스프링 자동 배치 (5단계)** |
| **속도 0 판정 애매함** | **해결** | **speedRatio <= 0.05 가 2초 지속 기준, DANGER UI로 인지 유도** |
| **속도/시간 단위 혼용** | **해결** | **speedRatio(0~1) 단일 기준 + 덧셈 연산 + /초·deltaTime 통일 (§3)** |
| 콤보 가속 폭주 (덧셈이라도 누적) | 중간 | speedRatio 1.0 클램프 + 마찰이 항상 작용 → 자연 감속으로 균형 |
| 추락/부활 카메라 어지러움 | 중간 | lerp 계수 조정, 추락 시 카메라 속도 제한 |
| 모바일 오발사 | 중간 | 드래그 최소 거리 threshold 20px |
| 파티클 과부하 | 중간 | 풀 300개 고정, 오래된 것 재활용 |
| 콤보 시스템 너무 어려움 | 중간 | 초반 타이밍 윈도우 충분히 넓게, config로 조정 |
| 카메라 추적 어지러움 | 낮음 | lerp 계수 조정 + 셰이크 강도 제한 |

---

## 요약

| 항목 | 내용 |
|------|------|
| **장르** | 2D 사이드뷰 무한 아케이드 |
| **목표** | 최대한 높이 + 멀리 (높이 우선) |
| **발사** | 슬링 드래그 — 당길수록 파워 증가, 놓으면 발사 |
| **조작** | 드래그 발사 + 착지 타이밍 클릭/탭 |
| **핵심 루프** | 슬링 발사 → 착지 → 타이밍 가속 → 갭 도약 → 반복 |
| **속도 역할** | 파괴력 + 도약력 동시 담당 |
| **탈락 조건** | 바다로 추락(SPLASH) or 속도 5% 이하 2초 지속 |
| **부활** | 추락 중 아래 섬 착지 시 가능 (바다 위이면 생존) |
| **클리어** | 달 고도(18,000px) 도달 → MOON REACHED! |
| **지형 형태** | Bowl(U자) / Ramp(경사) / Wave(S파도) — 모두 매끄러운 CatmullRom |
| **절차적 생성** | 진행도 기반 무한 섬 생성 (폭·깊이·갭 난이도 증가) |
| **물리** | ROLLING=자체 speedRatio 시스템, FLYING/FALLING=Planck.js |
| **WebGL 어필** | InstancedMesh 파티클, FBM 크레이터, Bloom, ChromaticAberration, 5단계 배경 셰이더(바다→우주→달) |

---

## 구현 현황 (2026-06-07 기준)

| 기능 | 상태 |
|------|------|
| 슬링 발사 시스템 (드래그, 고무줄 비주얼, 궤적 미리보기) | ✅ 완료 |
| 상태 머신 (TITLE→SLINGING→FLYING→ROLLING⇄FALLING→GAMEOVER) | ✅ 완료 |
| Bowl/Ramp/Wave 지형 + 절차적 무한 생성 | ✅ 완료 |
| Planck.js 비행·추락 물리 (중력, 충돌, CCD) | ✅ 완료 |
| speedRatio 기반 구름 물리 (마찰, 경사) | ✅ 완료 |
| 타이밍 판정 (PERFECT/GOOD/OK/MISS) + 콤보 | ✅ 완료 |
| 장애물 5종 + 파괴 판정 | ✅ 완료 |
| 지형 파괴 (크레이터 셰이더 + 파편 물리) | ✅ 완료 |
| InstancedMesh 파티클 시스템 (512슬롯) | ✅ 완료 |
| PostFX (Bloom + ChromaticAberration + Vignette) | ✅ 완료 |
| 배경 셰이더 (바다→밤하늘→우주→달 궤도 5단계, 별·은하수·바다 반짝임) | ✅ 완료 |
| 달 오브젝트 (크레이터 데코, 높이별 등장·확대) | ✅ 완료 |
| 바다 수면 레이어 (낮은 높이에서만 표시) | ✅ 완료 |
| SPLASH 낙사 / MOON REACHED 클리어 연출 | ✅ 완료 |
| HUD 달까지 거리 표시 (TO MOON) | ✅ 완료 |
| 카메라 셰이크 + 슬로우모션 | ✅ 완료 |
| 점수 시스템 + 베스트 기록 저장 (localStorage) | ✅ 완료 |
| 비행 중 아르마딜로 velocity 방향 회전 | ✅ 완료 |
| 모바일 터치 지원 | ✅ 완료 |
