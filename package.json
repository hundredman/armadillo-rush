{
  "name": "armadillo-rush",
  "version": "0.1.0",
  "type": "module",
  "description": "Drag the slingshot, time uphill boosts, and rush from the sea to the moon.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "stress:terrain": "node scripts/terrain-stress.mjs",
    "verify": "npm run stress:terrain && npm run build",
    "preview": "vite preview",
    "deploy": "npm run build && node scripts/deploy.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.108.0",
    "planck": "^1.5.0",
    "postprocessing": "^6.39.1",
    "three": "^0.171.0"
  }
}
